extends layout
block content
	h3 You are surfing someones profile
	if (isFollowing === -1)
		button.btn.btn-default(type='submit', action="/follow", method="post") Follow
	if (publicPosts.length > 0)
		table
			tbody
				for n in publicPosts
					tr
						td
							p= n.publicPost

